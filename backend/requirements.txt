# ===== 轻量多源数据同步 ETL SaaS 系统 - 后端依赖 =====
# 后端技术栈: FastAPI + SQLAlchemy + APScheduler + (MySQL/SQLite)

fastapi==0.110.0
uvicorn[standard]==0.27.1
sqlalchemy==2.0.25
pydantic==2.6.4
pydantic-settings==2.2.1
python-jose[cryptography]==3.3.0
bcrypt==4.1.2
python-multipart==0.0.9
APScheduler==3.10.4
cryptography==42.0.5

# 数据源驱动（按需安装；达梦 dmPython 需从官方安装包获取并安装到当前 Python 环境）
pymysql==1.1.0
psycopg2-binary==2.9.9
pyodbc==5.1.0
oracledb==2.2.1

# 可选：本地开发默认 SQLite，无需额外驱动
